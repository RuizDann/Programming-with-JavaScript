// This is a simple function which will make it easier to analyze when I write tests for it
function addFive(val) {
    return val + 5;
}

// added a line to export this function so that it can be used by other files in this project
module.exports = addFive;